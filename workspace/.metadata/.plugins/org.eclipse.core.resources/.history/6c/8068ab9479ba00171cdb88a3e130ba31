
public class caesarCode {

	private String caesarCode(String input, char offset) 
	{	
		char upper;
		if(Character.isUpperCase(offset)==false)
		{
		 upper = Character.toUpperCase(offset);
		}
		else { upper=offset;}
		
		
	}
}
